#create a function that takes length and width and finds the perimiter of rectangle

def find_perimeter(length, width):
    #logic to calculate the perimeter of a rectangle
    #formula is 2*length + 2*width
    return 2*length + 2*width

print(find_perimeter(1,2))